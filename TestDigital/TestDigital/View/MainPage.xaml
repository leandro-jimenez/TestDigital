<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TestDigital"
             x:Class="TestDigital.MainPage"
             x:Name="LoginView">

    <StackLayout Style="{StaticResource BaseStack}">
        <!-- Place new controls here -->
        <Entry x:Name="UserEntry" Style="{StaticResource BaseEntry}"/>
        <Entry x:Name="UserPwd" Style="{StaticResource BaseEntry}"/>
        <Button x:Name="LoginBtn" Clicked="OnNextPageButtonClicked" Style="{StaticResource ButtonStyle}"/>
    </StackLayout>

</ContentPage>
